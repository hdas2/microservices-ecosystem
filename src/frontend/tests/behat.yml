default:
  suites:
    canary:
      paths:
        - features/
      contexts:
        - FeatureContext
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost
      goutte: ~
      selenium2: ~